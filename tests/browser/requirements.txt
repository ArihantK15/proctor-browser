# Browser-level regression tests for the Procta dashboard.
# Install with:
#     pip install -r requirements.txt
#     playwright install chromium
pytest>=8.0
pytest-playwright>=0.5.0
playwright>=1.45.0
httpx>=0.27
